# Claude Rules - Правила работы с проектом Elbrus Arena

## 🗄️ Работа с Базой Данных

### При изменении схемы Prisma (schema.prisma):
1. **ВСЕГДА** откатываем все старые миграции и начинаем заново
2. Используем команды:
   ```bash
   npm run db:fresh  # Полный сброс + push + seed
   ```
3. В DEV-режиме используем `prisma db push`, а НЕ создаём миграции вручную
4. Миграции создаём только для продакшн деплоя через `prisma migrate dev`

### При создании/изменении сидов (seed.ts):
1. **ВСЕГДА** проверяем, что покрыты ВСЕ таблицы данных
2. Обязательные таблицы для сидов:
   - Users (тестовые пользователи)
   - Monsters (все монстры для подземелий)
   - Items (все предметы игры)
   - Dungeons (все уровни сложности)
   - DungeonMonster (связи монстров с подземельями)
   - MonsterLoot (таблицы дропа для каждого монстра)
3. После изменений запускаем `npm run db:fresh`
4. Проверяем логи: должно быть "✅ Сиды успешно загружены!"

## 🧪 Тестирование

### ВСЕГДА пишем тесты:
1. **Перед** реализацией функционала создаём тесты (TDD подход приветствуется)
2. **После** каждого изменения в коде запускаем тесты: `npm test`
3. Все тесты должны проходить перед коммитом
4. Текущий статус: 90/90 тестов должны проходить

### Структура тестов:
- `*.service.spec.ts` - для всех сервисов
- `*.controller.spec.ts` - для всех контроллеров
- `utils/*.spec.ts` - для утилитарных функций и калькуляторов

### При добавлении новых зависимостей в сервис:
1. Обновляем моки в соответствующих `*.spec.ts` файлах
2. Добавляем в `providers` моки для всех новых зависимостей
3. Проверяем, что тесты проходят

## 📋 Работа с Рефакторинг Планом

### При любых изменениях:
1. **ВСЕГДА** обращаемся к `REFACTORING_PLAN.txt`
2. Отмечаем выполненные задачи
3. Добавляем новые задачи, если обнаружены проблемы
4. Группируем задачи по модулям: Frontend, Backend, Shared, DevOps

### Формат записи:
```
[✓] Выполненная задача (модуль/название-файла.ts)
[ ] Невыполненная задача (модуль/название-файла.ts)
```

## ❓ Задавание Вопросов

### ВСЕГДА задаём вопросы когда:
1. Не уверены в подходе к реализации
2. Нужно выбрать между несколькими вариантами архитектуры
3. Изменения могут затронуть существующую логику
4. Баланс игры: урон, шансы дропа, стоимость, требования к предметам
5. Формулы расчётов: опыт, стамина, характеристики
6. Неясны требования к функционалу

### Формат вопросов:
- Чёткая формулировка проблемы
- Предложение 2-3 варианта решения
- Описание плюсов и минусов каждого варианта

## 🏗️ Архитектурные Принципы

### Backend (NestJS):
1. Каждый модуль должен быть самодостаточным
2. Используем Service Layer Pattern
3. Circular dependencies решаем через `forwardRef()`
4. Все бизнес-расчёты выносим в отдельные утилиты (`utils/`)
5. Validation через `class-validator` в DTO

### Frontend (React):
1. Компоненты делим на: pages, components, hooks
2. Стили держим рядом с компонентами
3. API вызовы только через `src/services/api.ts`
4. Типы импортируем из `shared/types/`

### Shared - ЕДИНЫЙ ИСТОЧНИК ИСТИНЫ:
**КРИТИЧЕСКИ ВАЖНО:** Shared типы - это ЕДИНСТВЕННЫЙ источник истины для всего проекта!

1. **ВСЕ** типы, энамы, интерфейсы, которые используются и на фронте, и на бэке → `shared/types/`
2. **НЕТ ДУБЛИРОВАНИЯ** - если тип уже есть в shared, НЕ создаём его заново
3. **Синхронизация обязательна** - при изменении shared типа проверяем все места использования

### Frontend - Отдельный источник для фронтовых типов:
1. `frontend/src/types/` - ТОЛЬКО для типов, которые используются исключительно на фронте
2. Примеры фронтовых типов: состояния UI, пропсы компонентов, локальные хелперы
3. Если тип нужен и на бэке - переносим в shared!

## ✨ ПРОСТОТА, ЧИСТОТА, ОТВЕТСТВЕННОСТЬ

**ЭТО КЛЮЧЕВОЙ ПРИНЦИП ПРОЕКТА!**

### ПРОСТОТА КОНЦЕПЦИЙ - КЛЮЧЕВОЕ:
1. **Простой код лучше сложного** - если можно написать проще, пиши проще
2. **Один файл = одна ответственность** - сервис делает одно дело, но делает хорошо
3. **Понятные имена** - название должно описывать суть, не нужно читать код
4. **Минимум абстракций** - не создаём абстракции "на будущее"

### ЧИСТОТА КОДА:
1. **НЕТ ДУБЛИРОВАНИЯ** - если код повторяется 3 раза, выносим в функцию/утилиту
2. **НЕТ мёртвого кода** - удаляем неиспользуемые импорты, функции, переменные
3. **НЕТ магических чисел** - все константы выносим в константы с понятными именами
4. **НЕТ длинных функций** - максимум 50 строк, если больше - разбиваем

### РАЗДЕЛЕНИЕ ОТВЕТСТВЕННОСТИ:
1. **Сервисы** - бизнес-логика (НЕ содержат UI логику)
2. **Контроллеры** - обработка HTTP запросов (НЕ содержат бизнес-логику)
3. **Утилиты** - чистые функции для расчётов (НЕ зависят от сервисов)
4. **Компоненты** - отображение UI (НЕ содержат бизнес-логику)

### ОБЯЗАТЕЛЬНЫЙ ОБЗОР ПАПОК ОТ А ДО Я:
**ПЕРЕД началом любой задачи:**
1. Открываем папку модуля и смотрим ВСЕ файлы в ней
2. Проверяем, нет ли уже готовой функции/сервиса для нашей задачи
3. Смотрим, как организован код в существующих файлах
4. СЛЕДУЕМ существующим паттернам и структуре

**Пример обзора для добавления функционала в Character:**
```
backend/src/character/
├── character.module.ts       ← смотрим, какие сервисы уже есть
├── character.controller.ts   ← смотрим, какие endpoints уже есть
├── character.service.ts      ← смотрим, какие методы уже есть
├── character-levelup.service.ts
├── character-stamina.service.ts
└── utils/
    └── stats-calculator.ts   ← смотрим, какие утилиты есть
```

**КРИТИЧНО:** НЕ создаём дубликаты функций! Если функция уже есть - используем её!

## 📝 Коммиты и Git

### Перед коммитом:
1. Запустить тесты: `npm test`
2. Проверить сборку: `npm run build`
3. Обновить REFACTORING_PLAN.txt
4. Проверить, что сиды загружаются: `npm run db:seed`

### Формат коммитов:
```
<type>: <краткое описание>

<детальное описание изменений>
```

Types: feat, fix, test, refactor, docs, style, chore

## 🎮 Game Balance (Баланс Игры)

### Текущие значения:
- **Level-up**: 100 * level² опыта, +3 очка характеристик за уровень
- **Stamina**: 100 максимум, восстановление 1/секунду, стоимость боя 20
- **Enhancement**: 100 золота, 20% шанс успеха, +10% к статам за уровень заточки

### При изменении баланса:
1. Документируем в этом файле
2. Обновляем константы в сервисах
3. Обновляем тесты с новыми значениями
4. Согласовываем с командой

## 🚀 Deployment

### DEV окружение:
- `npm run db:push` - синхронизация схемы
- `npm run db:fresh` - полный сброс БД
- `npm run start:dev` - запуск в dev режиме

### PROD окружение:
- `npm run db:migrate:deploy` - применение миграций
- `npm run build` - сборка проекта
- `npm run start:prod` - запуск в production

---

**Важно:** Эти правила созданы для обеспечения качества кода и предотвращения ошибок. Следуй им строго!