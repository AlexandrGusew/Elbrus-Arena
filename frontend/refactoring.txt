═══════════════════════════════════════════════════════════════════
🔥 FRONTEND РЕФАКТОРИНГ - КРИТИЧЕСКИЕ ПРОБЛЕМЫ 🔥
═══════════════════════════════════════════════════════════════════

📊 СТАТИСТИКА:
- ~15000+ строк МЕРТВОГО кода
- 6 ПАР дублирующихся компонентов
- 52 использования localStorage БЕЗ централизации
- 85 useState в страницах (где глобальный store?!)
- God components до 750 строк
- Props drilling на 3-4 уровня

═══════════════════════════════════════════════════════════════════
🚨 КРИТИЧНО - УДАЛИТЬ НЕМЕДЛЕННО! 🚨
═══════════════════════════════════════════════════════════════════

1. ПАПКА /figmareference/ - 10000+ СТРОК МУСОРА
   ❌ Вся папка src/figmareference/ - полный дубль компонентов
   ❌ 40+ UI компонентов shadcn/ui НЕ используются
   ❌ Свой vite.config, tailwind.config, App.tsx
   ❌ Дубли: LoginPage, DashboardPage, InventorySection, ForgeSection, CreateCharacterPage
   → УДАЛИТЬ ВСЮ ПАПКУ НАХРЕН

2. ДУБЛИРУЮЩИЕСЯ КОМПОНЕНТЫ
   ❌ ChatWindow.tsx (750 строк) + ChatWindow.old.tsx (588 строк)
   ❌ pages/Login.tsx + pages/login/LoginPage.tsx
   ❌ pages/CreateCharacter.tsx + pages/createCharacter/CreateCharacterPage.tsx
   → УДАЛИТЬ СТАРЫЕ ВЕРСИИ

3. МЕРТВЫЕ ФАЙЛЫ
   ❌ App.tsx (178 строк) - не используется в роутинге
   ❌ hooks/useTelegramWebApp.ts - старый хук
   ❌ services/auth.service.ts - заменен на RTK Query
   ❌ styles/common.styles.ts - константы определены, но НЕ используются
   ❌ components/GameViewport.tsx - нигде не импортируется
   → ВСЕ В МУСОРКУ

═══════════════════════════════════════════════════════════════════
⚠️ ВЫСОКИЙ ПРИОРИТЕТ - АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ ⚠️
═══════════════════════════════════════════════════════════════════

4. GOD COMPONENTS - МОНСТРЫ ПО 600+ СТРОК
   🔴 Dashboard.tsx (603 строки):
      - Музыка (audioRef, crossfade - 100+ строк)
      - Чат (isChatOpen)
      - Выбор персонажа (characterId, autoCreate)
      - Секции (activeSection, showForge)
      - Предметы (selectedItem, forgeItemSlot)
      - Обновление имен
      - Logout логика
      - Тестовые функции
   → РАЗБИТЬ на хуки: useMusic, useCharacterSelection, useAuth

   🔴 Dungeon.tsx (387 строк):
      - Выбор сложности
      - Музыка
      - Чат
      - WebSocket боя
      - Обновление кэша
      - ДВА разных UI (selection + battle)
   → РАЗБИТЬ на DungeonSelection + DungeonBattle

   🔴 InventorySection.tsx (652 строки):
      - Drag & Drop
      - Экипировка/снятие
      - Выбор предметов
      - Кузница
      - Бонусы заточки
      - Маппинг иконок
      - Рендер UI
   → ВЫНЕСТИ логику в хуки: useItemDragDrop, useItemEquipment, useItemEnhancement

   🔴 ChatWindow.tsx (750 строк):
      - WebSocket
      - Табы (All/Party/Whisper)
      - Сообщения
      - Пользователи
      - Отправка
   → РАЗБИТЬ на ChatTabs, ChatMessages, ChatInput, ChatUsers

5. ДУБЛИРОВАНИЕ API
   🔴 characterApi.ts:
      - getMyCharacter: query => /character/me (возвращает массив)
      - getMyCharacters: query => /character/my-characters (возвращает массив)
   → ДВА МЕТОДА ДЕЛАЮТ ОДНО И ТО ЖЕ!

   🔴 authApi.ts:
      - checkAuth query не используется НИГДЕ
   → УДАЛИТЬ

6. LOCALSTORAGE АД (52 использования!)
   🔴 characterId хранится в localStorage + useState
   🔴 isAuthenticated хранится в localStorage
   🔴 musicPlaying хранится в localStorage
   🔴 Каждый компонент управляет своей копией
   🔴 НЕТ централизованного управления

   Где это используется:
   - authApi.ts (5 мест)
   - Dashboard.tsx (characterId + musicPlaying)
   - Dungeon.tsx (читает characterId)
   - ProtectedRoute.tsx (isAuthenticated)
   - CreateCharacter.tsx (musicPlaying)
   - AuthLayout.tsx (musicPlaying)

   → СОЗДАТЬ centralized store или Context API

7. МУЗЫКА - ПОВТОРЯЮЩИЙСЯ КОД
   🔴 КАЖДАЯ страница управляет музыкой отдельно:
      - Dashboard.tsx
      - CreateCharacter.tsx
      - AuthLayout.tsx
      - Dungeon.tsx
      - PvP.tsx
   🔴 Одинаковый код в 5 местах!
   → ВЫНЕСТИ в MusicProvider context

═══════════════════════════════════════════════════════════════════
⚡ СРЕДНИЙ ПРИОРИТЕТ - КАЧЕСТВО КОДА ⚡
═══════════════════════════════════════════════════════════════════

8. СТИЛИ - ХАОС
   🟡 Dashboard.tsx: 600+ строк inline стилей
   🟡 LoginPage.tsx: Tailwind классы
   🟡 CreateCharacterPage.tsx: MIX inline + Tailwind
   🟡 styles/dashboard.styles.ts используется
   🟡 styles/common.styles.ts НЕ используется
   → ВЫБРАТЬ ЕДИНЫЙ ПОДХОД (рекомендую Tailwind)

9. PROPS DRILLING
   🟡 Dashboard → InventorySection → ForgeSection (3 уровня)
   🟡 Передается 6-8 пропсов через каждый уровень
   🟡 selectedItem, onItemSelect, forgeItemSlot, onForgeItemSelect, etc
   → ИСПОЛЬЗОВАТЬ Context API или Redux

10. ТИПИЗАЦИЯ
   🟡 Dashboard.tsx: const [forgeItemSlot, setForgeItemSlot] = useState<any | null>(null)
   🟡 Использование 'any' вместо нормальных типов
   → ТИПИЗИРОВАТЬ ВСЕ!

11. СТРУКТУРА ПАПОК - НЕСОГЛАСОВАННОСТЬ
   🟡 Старые страницы в /pages/: Login.tsx, CreateCharacter.tsx
   🟡 Новые страницы в /pages/login/, /pages/createCharacter/
   🟡 Остальные напрямую в /pages/: Dashboard.tsx, Dungeon.tsx, PvP.tsx
   → ПРИВЕСТИ К ЕДИНОЙ СТРУКТУРЕ:
      /pages/login/
      /pages/register/
      /pages/dashboard/
      /pages/dungeon/
      /pages/pvp/

12. СТЕЙТ МЕНЕДЖМЕНТ
   🟡 85 useState в pages/
   🟡 Локальное состояние вместо глобального
   🟡 Чат теряется при переходе между страницами
   🟡 useChat hook - WebSocket + локальное состояние БЕЗ Redux
   → ИНТЕГРИРОВАТЬ с Redux или Context

═══════════════════════════════════════════════════════════════════
📋 ПЛАН ДЕЙСТВИЙ 📋
═══════════════════════════════════════════════════════════════════

ФАЗА 1 - ОЧИСТКА (1-2 часа):
✓ 1. Удалить /figmareference/ полностью
✓ 2. Удалить ChatWindow.old.tsx
✓ 3. Удалить pages/Login.tsx, pages/CreateCharacter.tsx
✓ 4. Удалить App.tsx, useTelegramWebApp.ts, auth.service.ts
✓ 5. Удалить styles/common.styles.ts, components/GameViewport.tsx

ФАЗА 2 - API (30 минут):
✓ 6. Объединить getMyCharacter и getMyCharacters в один метод
✓ 7. Удалить неиспользуемый checkAuth
✓ 8. Типизировать все 'any' типы

ФАЗА 3 - ГЛОБАЛЬНОЕ СОСТОЯНИЕ (2-3 часа):
✓ 9. Создать MusicProvider context (вместо 5 копий кода)
✓ 10. Создать AuthProvider context (для localStorage auth)
✓ 11. Вынести characterId в Redux или Context
✓ 12. Интегрировать useChat с Redux

ФАЗА 4 - РАЗБИЕНИЕ КОМПОНЕНТОВ (3-4 часа):
✓ 13. Dashboard.tsx:
    - Вынести useMusic hook
    - Вынести useCharacterSelection hook
    - Разбить UI на DashboardLayout + секции
✓ 14. Dungeon.tsx:
    - DungeonSelection component
    - DungeonBattle component
✓ 15. InventorySection.tsx:
    - useItemDragDrop hook
    - useItemEquipment hook
    - useItemEnhancement hook
✓ 16. ChatWindow.tsx:
    - ChatTabs component
    - ChatMessages component
    - ChatInput component
    - ChatUsers component

ФАЗА 5 - УНИФИКАЦИЯ (1-2 часа):
✓ 17. Привести все стили к Tailwind
✓ 18. Убрать props drilling через Context API
✓ 19. Унифицировать структуру папок pages/

═══════════════════════════════════════════════════════════════════
💰 ИТОГОВАЯ ЭКОНОМИЯ 💰
═══════════════════════════════════════════════════════════════════

После рефакторинга:
✅ ~15000 строк удаленного кода
✅ 6 дублей устранено
✅ Централизованное управление state
✅ Единый подход к стилям
✅ Компоненты < 200 строк
✅ Переиспользуемая логика в hooks
✅ Нет props drilling
✅ Типобезопасность

ВРЕМЯ НА РЕФАКТОРИНГ: 7-10 часов работы
ВЫГОДА: Чистый, поддерживаемый код на годы вперед!

═══════════════════════════════════════════════════════════════════
