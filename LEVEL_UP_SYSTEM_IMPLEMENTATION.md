# План реализации системы распределения очков характеристик

## Описание задачи

Реализовать систему распределения свободных очков (`freePoints`) по характеристикам персонажа в Dashboard. При клике на прогресс-бар уровня должна открываться секция LevelUp в правой колонке Dashboard, где игрок может распределять очки между 5 характеристиками: Сила, Ловкость, Интеллект, HP, Выносливость.

## Текущее состояние

- ✅ Распределение очков работает только для: Сила, Ловкость, Интеллект
- ✅ Есть отдельная страница `/levelup` с функционалом распределения
- ✅ Бэкенд метод `distributeStats` принимает только 3 параметра
- ❌ Нет интеграции в Dashboard
- ❌ Нет возможности распределять очки в HP и Stamina

## Целевое состояние

- ✅ Кликабельный прогресс-бар уровня в CharacterCard
- ✅ Секция LevelUp в правой колонке Dashboard
- ✅ Распределение очков по 5 характеристикам: STR, AGI, INT, HP, Stamina
- ✅ Сохранение изменений в БД
- ✅ Предпросмотр изменений перед применением

---

## Шаг 1: Расширение бэкенда для поддержки HP и Stamina

### 1.1. Обновить метод `distributeStats` в `character-levelup.service.ts`

**Файл:** `backend/src/character/character-levelup.service.ts`

**Изменения:**
```typescript
async distributeStats(
  characterId: number,
  strength: number,
  agility: number,
  intelligence: number,
  maxHp?: number,      // НОВОЕ: очки для HP
  stamina?: number,    // НОВОЕ: очки для выносливости
): Promise<void>
```

**Логика:**
- Добавить `maxHp` и `stamina` в подсчет `totalPoints`
- Добавить валидацию для новых параметров (не отрицательные)
- Обновить персонажа в БД:
  - `strength += strength`
  - `agility += agility`
  - `intelligence += intelligence`
  - `maxHp += maxHp` (если передан)
  - `stamina += stamina` (если передан)
  - `freePoints -= totalPoints`

**Вопросы для решения:**
- Сколько HP добавляется за 1 очко? (предложение: +10 HP за очко)
- Сколько Stamina добавляется за 1 очко? (предложение: +5 Stamina за очко)

### 1.2. Обновить контроллер `character.controller.ts`

**Файл:** `backend/src/character/character.controller.ts`

**Изменения:**
```typescript
@Post(':id/distribute-stats')
async distributeStats(
  @Param('id') id: string,
  @Body() body: { 
    strength: number; 
    agility: number; 
    intelligence: number;
    maxHp?: number;      // НОВОЕ
    stamina?: number;     // НОВОЕ
  },
): Promise<void> {
  return this.levelUpService.distributeStats(
    Number(id),
    body.strength,
    body.agility,
    body.intelligence,
    body.maxHp,          // НОВОЕ
    body.stamina,        // НОВОЕ
  );
}
```

---

## Шаг 2: Обновление API клиента на фронтенде

### 2.1. Обновить `characterApi.ts`

**Файл:** `frontend/src/store/api/characterApi.ts`

**Изменения:**
```typescript
distributeStats: builder.mutation<
  void,
  { 
    characterId: number; 
    strength: number; 
    agility: number; 
    intelligence: number;
    maxHp?: number;      // НОВОЕ
    stamina?: number;    // НОВОЕ
  }
>({
  query: ({ characterId, ...stats }) => ({
    url: `/character/${characterId}/distribute-stats`,
    method: 'POST',
    body: stats,
  }),
  invalidatesTags: (result, error, { characterId }) => [
    { type: 'Character', id: characterId },
  ],
}),
```

---

## Шаг 3: Создание компонента LevelUpSection для Dashboard

### 3.1. Создать новый компонент `LevelUpSection.tsx`

**Файл:** `frontend/src/components/dashboard/LevelUpSection.tsx`

**Структура компонента:**
- Пропсы: `character`, `onBack` (для возврата на главную)
- Состояние для временных очков: `pendingStr`, `pendingAgi`, `pendingInt`, `pendingHp`, `pendingStamina`
- Загрузка данных: `useGetLevelProgressQuery` для получения `freePoints`
- UI элементы:
  - Заголовок "Распределение характеристик"
  - Блок с количеством доступных очков
  - 5 строк для характеристик (каждая с кнопками +/-)
  - Предпросмотр изменений (опционально)
  - Кнопки "Сбросить" и "Применить"
  - Кнопка "Назад"

**Стиль:**
- Использовать стили, аналогичные `InventorySection` и `ForgeSection`
- Соответствовать общему дизайну Dashboard

---

## Шаг 4: Интеграция в CharacterCard

### 4.1. Добавить кликабельность прогресс-бара уровня

**Файл:** `frontend/src/components/dashboard/CharacterCard.tsx`

**Изменения:**
- Добавить проп `onLevelBarClick?: () => void`
- Добавить `onClick` и `cursor: pointer` на прогресс-бар (строки 244-261)
- Добавить hover эффект для визуальной обратной связи

**Код:**
```typescript
<div 
  className="border-2 border-amber-800/40 rounded bg-gradient-to-b from-stone-950/50 to-black/50 overflow-hidden relative cursor-pointer hover:border-amber-600/60 transition-all"
  onClick={onLevelBarClick}
  style={{ minHeight: '32px' }}
>
  {/* ... existing code ... */}
</div>
```

---

## Шаг 5: Интеграция в Dashboard

### 5.1. Добавить состояние для секции LevelUp

**Файл:** `frontend/src/pages/Dashboard.tsx`

**Изменения:**
- Расширить тип `activeSection`: `'main' | 'inventory' | 'levelup'`
- Добавить обработчик `handleLevelBarClick` для открытия секции LevelUp

### 5.2. Добавить отображение LevelUpSection

**Файл:** `frontend/src/pages/Dashboard.tsx`

**Изменения:**
- В правой колонке (RIGHT COLUMN) добавить условие для `activeSection === 'levelup'`
- Отображать `LevelUpSection` вместо `NavigationButtons` или `InventorySection`
- Передать пропсы: `character`, `onBack={() => setActiveSection('main')}`

### 5.3. Передать обработчик в CharacterCard

**Файл:** `frontend/src/pages/Dashboard.tsx`

**Изменения:**
- В компоненте `CharacterCard` добавить проп:
  ```typescript
  <CharacterCard 
    character={character}
    selectedItem={selectedItem}
    onItemSelect={setSelectedItem}
    onLevelBarClick={() => {
      setActiveSection('levelup');
      setShowForge(false);
    }}
  />
  ```

---

## Шаг 6: Реализация UI для распределения очков

### 6.1. Структура LevelUpSection

**Компоненты:**
1. **Заголовок секции** - "Распределение характеристик"
2. **Блок с доступными очками** - показывает `freePoints - pointsUsed`
3. **Список характеристик** (5 строк):
   - 💪 Сила: `{character.strength}` + `{pendingStr > 0 ? ` +${pendingStr}` : ''}`
   - 🏃 Ловкость: `{character.agility}` + `{pendingAgi > 0 ? ` +${pendingAgi}` : ''}`
   - 🧠 Интеллект: `{character.intelligence}` + `{pendingInt > 0 ? ` +${pendingInt}` : ''}`
   - ❤️ HP: `{character.maxHp}` + `{pendingHp > 0 ? ` +${pendingHp * 10}` : ''}` (если +10 HP за очко)
   - ⚡ Выносливость: `{character.stamina}` + `{pendingStamina > 0 ? ` +${pendingStamina * 5}` : ''}` (если +5 Stamina за очко)
4. **Кнопки управления** - "Сбросить" и "Применить"
5. **Кнопка "Назад"** - возврат на главную

### 6.2. Логика распределения

**Состояние:**
```typescript
const [pendingStr, setPendingStr] = useState(0);
const [pendingAgi, setPendingAgi] = useState(0);
const [pendingInt, setPendingInt] = useState(0);
const [pendingHp, setPendingHp] = useState(0);
const [pendingStamina, setPendingStamina] = useState(0);
```

**Подсчет:**
```typescript
const pointsUsed = pendingStr + pendingAgi + pendingInt + pendingHp + pendingStamina;
const pointsRemaining = levelProgress.freePoints - pointsUsed;
```

**Обработчики:**
- `handleIncrement` - увеличивает очки, если `pointsRemaining > 0`
- `handleDecrement` - уменьшает очки, если текущее значение > 0
- `handleReset` - сбрасывает все временные очки
- `handleApply` - отправляет запрос на бэкенд и применяет изменения

---

## Шаг 7: Обновление логики применения очков

### 7.1. Обработка HP и Stamina при применении

**В LevelUpSection:**
```typescript
const handleApply = async () => {
  if (pointsUsed === 0) {
    alert('Распределите хотя бы одно очко!');
    return;
  }

  try {
    await distributeStats({
      characterId: character.id,
      strength: pendingStr,
      agility: pendingAgi,
      intelligence: pendingInt,
      maxHp: pendingHp,        // НОВОЕ
      stamina: pendingStamina, // НОВОЕ
    }).unwrap();

    // Сброс временных очков
    setPendingStr(0);
    setPendingAgi(0);
    setPendingInt(0);
    setPendingHp(0);
    setPendingStamina(0);

    alert('Характеристики успешно улучшены!');
  } catch (error: any) {
    alert(`Ошибка: ${error.data?.message || 'Не удалось распределить очки'}`);
  }
};
```

---

## Шаг 8: Предпросмотр изменений (опционально)

### 8.1. Расчет новых значений

**Использовать StatsCalculator для предпросмотра:**
- Текущие характеристики: `StatsCalculator.calculateEffectiveStats(character)`
- Новые характеристики: рассчитать с учетом распределенных очков
- Показать разницу в UI (зеленым цветом)

---

## Шаг 9: Обработка обновления HP при изменении maxHp

### 9.1. Обновление currentHp при увеличении maxHp

**В бэкенде `distributeStats`:**
- Если `maxHp` увеличивается, можно также увеличить `currentHp` пропорционально
- Или оставить `currentHp` без изменений (игрок должен будет восстановить HP)

**Вопрос:** Что делать с `currentHp` при увеличении `maxHp`?
- Вариант 1: Оставить `currentHp` без изменений
- Вариант 2: Увеличить `currentHp` пропорционально (например, если maxHp увеличился на 10%, то currentHp тоже)

---

## Шаг 10: Тестирование

### 10.1. Проверка функционала

1. ✅ Клик на прогресс-бар открывает секцию LevelUp
2. ✅ Отображается количество доступных очков
3. ✅ Можно распределять очки по всем 5 характеристикам
4. ✅ Валидация работает (нельзя распределить больше очков, чем доступно)
5. ✅ Кнопка "Применить" сохраняет изменения в БД
6. ✅ Кнопка "Сбросить" очищает временные очки
7. ✅ Кнопка "Назад" возвращает на главную
8. ✅ После применения данные персонажа обновляются
9. ✅ HP и Stamina корректно увеличиваются

---

## Порядок реализации

1. **Шаг 1** - Расширить бэкенд (метод `distributeStats`)
2. **Шаг 2** - Обновить API клиент на фронтенде
3. **Шаг 3** - Создать компонент `LevelUpSection.tsx`
4. **Шаг 4** - Добавить кликабельность прогресс-бара в `CharacterCard`
5. **Шаг 5** - Интегрировать в Dashboard
6. **Шаг 6** - Реализовать UI для всех 5 характеристик
7. **Шаг 7** - Реализовать логику применения
8. **Шаг 8** - Добавить предпросмотр (опционально)
9. **Шаг 9** - Обработать обновление currentHp (если нужно)
10. **Шаг 10** - Тестирование

---

## Вопросы для уточнения

1. **Сколько HP добавляется за 1 очко?**
   - Предложение: +10 HP за очко
   - Альтернатива: +5 HP за очко

2. **Сколько Stamina добавляется за 1 очко?**
   - Предложение: +5 Stamina за очко
   - Альтернатива: +10 Stamina за очко

3. **Что делать с currentHp при увеличении maxHp?**
   - Оставить без изменений (игрок восстанавливает HP сам)
   - Увеличить пропорционально

4. **Нужен ли предпросмотр изменений?**
   - Да, показывать как изменятся характеристики
   - Нет, достаточно просто распределения

5. **Максимальные значения HP и Stamina?**
   - Есть ли лимиты?
   - Или можно увеличивать бесконечно?

---

## Примечания

- Все изменения должны быть обратно совместимыми (старый функционал должен продолжать работать)
- Валидация должна работать как на фронтенде, так и на бэкенде
- UI должен соответствовать общему стилю Dashboard
- После применения изменений данные персонажа должны автоматически обновляться через RTK Query

