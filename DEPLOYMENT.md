# Инструкция по деплою MVP на сервер

Эта инструкция описывает деплой ветки `dpl` (MVP из `develop` с исправлениями из `deploy`) на продакшн сервер.

## Предварительные требования

- Сервер с Ubuntu/Debian
- Установленный Docker и Docker Compose
- Настроенный домен (например, nightfall-arena.ru)
- База данных PostgreSQL (может быть на отдельном сервере)
- SSL сертификат (рекомендуется через Let's Encrypt/Certbot)

## Шаг 1: Подключение к серверу

```bash
ssh root@YOUR_SERVER_IP
```

## Шаг 2: Установка зависимостей (если еще не установлены)

```bash
# Обновляем пакеты
apt update && apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Устанавливаем Docker Compose plugin
apt install docker-compose-plugin -y

# Проверяем установку
docker --version
docker compose version
```

## Шаг 3: Клонирование проекта

```bash
# Создаем директорию для проекта
mkdir -p /var/www
cd /var/www

# Клонируем репозиторий и переключаемся на ветку dpl
git clone https://github.com/AlexandrGusew/Elbrus-Arena.git app
cd app
git checkout dpl
```

## Шаг 4: Настройка переменных окружения

```bash
# Создаем файл .env на основе примера
cp .env.example .env

# Редактируем .env
nano .env
```

Заполните следующие переменные:

```env
# Database (укажите данные вашей БД)
DATABASE_URL=postgresql://username:password@db_host:5432/database_name

# JWT Secret (сгенерируйте случайную строку минимум 32 символа)
JWT_SECRET=ваш_супер_секретный_ключ_минимум_32_символа
JWT_EXPIRES_IN=7d

# Application
NODE_ENV=production
PORT=3000

# Frontend URL (ваш домен)
FRONTEND_URL=https://nightfall-arena.ru
CORS_ORIGINS=https://nightfall-arena.ru

# API URLs для frontend (используются при сборке Docker)
VITE_API_URL=https://nightfall-arena.ru/api
VITE_WS_URL=wss://nightfall-arena.ru
```

**Важно:**
- Используйте `https://` и `wss://` если у вас настроен SSL
- `DATABASE_URL` должен указывать на вашу PostgreSQL базу
- `JWT_SECRET` должен быть уникальным и сложным

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

## Шаг 5: Инициализация базы данных

```bash
# Запускаем временный контейнер backend для миграций
docker compose run --rm backend npx prisma migrate deploy

# Заполняем БД начальными данными (seed)
docker compose run --rm backend npx prisma db seed
```

## Шаг 6: Сборка и запуск контейнеров

```bash
# Очищаем старые образы (если есть)
docker system prune -a -f

# Собираем образы
docker compose build --no-cache

# Запускаем контейнеры в фоновом режиме
docker compose up -d

# Проверяем логи
docker compose logs -f --tail=50
```

Дождитесь, пока backend запустится (обычно 30-60 секунд).

## Шаг 7: Проверка работы

```bash
# Проверяем статус контейнеров
docker compose ps

# Проверяем API
curl http://localhost:3000/api

# Проверяем frontend
curl http://localhost:8080
```

Если всё работает локально, проверьте через домен:

```bash
curl https://nightfall-arena.ru/api
```

## Шаг 8: Настройка автозапуска

Контейнеры уже настроены на автоматический перезапуск (`restart: unless-stopped` в docker-compose.yml), поэтому после перезагрузки сервера они запустятся автоматически.

## Полезные команды

```bash
# Просмотр логов
docker compose logs -f

# Перезапуск контейнеров
docker compose restart

# Остановка контейнеров
docker compose down

# Обновление кода из Git
git pull origin dpl
docker compose build --no-cache
docker compose up -d

# Просмотр статуса
docker compose ps
docker stats
```

## Устранение проблем

### Проблема: Контейнер backend не запускается

```bash
# Проверьте логи
docker compose logs backend

# Проверьте подключение к БД
docker compose exec backend npx prisma db pull
```

### Проблема: Frontend показывает ошибки CORS/Mixed Content

Убедитесь, что в `.env`:
- `VITE_API_URL` использует правильный протокол (http/https)
- `CORS_ORIGINS` включает ваш домен

Затем пересоберите frontend:

```bash
docker compose build --no-cache frontend
docker compose up -d
```

### Проблема: Медленная сборка

Это нормально для первой сборки. Последующие будут быстрее благодаря кэшированию Docker.

## Обновление проекта

```bash
cd /var/www/app
git pull origin dpl
docker compose build
docker compose up -d
```

## Откат к предыдущей версии

```bash
cd /var/www/app
git log --oneline  # Найдите нужный commit
git checkout COMMIT_HASH
docker compose build --no-cache
docker compose up -d
```

---

**Готово!** Ваш MVP должен быть запущен и работать на https://nightfall-arena.ru 🎉
