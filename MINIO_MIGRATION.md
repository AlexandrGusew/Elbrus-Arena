# Миграция медиа-файлов на MinIO

## 🎯 Цели оптимизации

### Текущая ситуация (ДО)
- ❌ Размер Docker образа фронтенда: ~150MB (включая 63MB медиа)
- ❌ Время сборки: 3-5 минут (копирование 63MB assets)
- ❌ Все медиа в bundle - долгая первая загрузка
- ❌ Обновление картинки = пересборка всего образа

### После миграции на MinIO (ПОСЛЕ)
- ✅ Размер Docker образа: ~15MB (без медиа)
- ✅ Время сборки: 30-60 секунд
- ✅ Lazy loading - медиа загружаются по требованию
- ✅ Обновление медиа без пересборки - просто загрузить в MinIO
- ✅ CDN-подобное кеширование медиа
- ✅ Версионирование медиа файлов

## 📦 Инвентаризация медиа

### Большие файлы (приоритет для MinIO):
```
21MB  - choosePlayer/animatedBackground.mp4
15MB  - pvpArena/pvpArena2.mp4
6.6MB - mainCity/mainCityBackground.mp4
1.7MB - enterDungeon/dungeons/medium/medium_level_background.png
1.4MB - enterDungeon/dungeons/hard/hard_level_background.png
1.3MB - pvpArena/pvpArenaBackground.mp4
1.2MB - enterDungeon/enterDungeonBackground.mp4
1.2MB - choosePlayer/background.png
1.1MB - mainCity/lvlup.png
```

**Итого: ~53MB видео + ~10MB изображений = 63MB**

## 🏗️ Архитектура решения

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │
       ├─────────────┐
       │             │
       v             v
┌─────────┐    ┌──────────┐
│  Nginx  │    │  MinIO   │
│  :80    │    │  :9000   │
└────┬────┘    └──────────┘
     │              │
     v              │
┌─────────┐         │
│ Backend │         │
│  :3000  │         │
└─────────┘         │
                    │
              ┌─────┴─────┐
              │  Assets   │
              │  Bucket   │
              └───────────┘
```

## 🚀 План миграции (пошагово)

### Этап 1: Настройка MinIO (на сервере)

#### 1.1 Создать bucket для assets
```bash
# Установить MinIO Client (mc)
wget https://dl.min.io/client/mc/release/linux-amd64/mc
chmod +x mc
sudo mv mc /usr/local/bin/

# Настроить подключение к локальному MinIO
mc alias set myminio http://localhost:9000 YOUR_ACCESS_KEY YOUR_SECRET_KEY

# Создать bucket
mc mb myminio/elbrus-arena-assets

# Настроить публичный доступ для чтения
mc anonymous set download myminio/elbrus-arena-assets
```

#### 1.2 Настроить CORS для MinIO
Создать файл `cors.json`:
```json
{
  "CORSRules": [
    {
      "AllowedOrigins": ["https://nightfall-arena.ru", "http://localhost:5173"],
      "AllowedMethods": ["GET", "HEAD"],
      "AllowedHeaders": ["*"],
      "MaxAgeSeconds": 3600
    }
  ]
}
```

Применить CORS:
```bash
mc cors set cors.json myminio/elbrus-arena-assets
```

### Этап 2: Загрузка медиа в MinIO

#### 2.1 Использовать скрипт миграции
```bash
# На локальной машине (где есть frontend/src/assets)
node scripts/upload-assets-to-minio.js

# Или вручную через mc:
mc cp --recursive frontend/src/assets/ myminio/elbrus-arena-assets/
```

#### 2.2 Проверка загрузки
```bash
mc ls myminio/elbrus-arena-assets/
mc ls myminio/elbrus-arena-assets/choosePlayer/
```

### Этап 3: Обновление кода фронтенда

#### 3.1 Добавить переменную окружения
В `.env.example` и `.env`:
```env
VITE_MINIO_URL=http://localhost:9000
VITE_MINIO_BUCKET=elbrus-arena-assets
```

В production (на сервере):
```env
VITE_MINIO_URL=https://nightfall-arena.ru/minio
VITE_MINIO_BUCKET=elbrus-arena-assets
```

#### 3.2 Создать утилиту для URL
Создан файл `frontend/src/utils/assetUrl.ts`

#### 3.3 Обновить импорты (постепенно)
Заменить:
```typescript
import backgroundVideo from '../assets/choosePlayer/animatedBackground.mp4';
```

На:
```typescript
import { getAssetUrl } from '../utils/assetUrl';
const backgroundVideo = getAssetUrl('choosePlayer/animatedBackground.mp4');
```

### Этап 4: Оптимизация Docker образа

#### 4.1 Исключить assets из Docker
Обновить `.dockerignore`:
```
frontend/src/assets/**/*.mp4
frontend/src/assets/**/*.mp3
frontend/src/assets/**/*.png
frontend/src/assets/**/*.jpg
# Оставить только мелкие иконки если нужно
!frontend/src/assets/**/*icon*.png
```

#### 4.2 Обновить Vite конфиг
Настроить external для assets (опционально)

### Этап 5: Настройка Nginx для проксирования MinIO

#### 5.1 Добавить в nginx.conf
```nginx
# Проксирование запросов к MinIO
location /minio/ {
    proxy_pass http://localhost:9000/elbrus-arena-assets/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;

    # Кеширование статики на 30 дней
    expires 30d;
    add_header Cache-Control "public, immutable";

    # CORS headers (если MinIO CORS не работает)
    add_header Access-Control-Allow-Origin *;
}
```

## 📊 Метрики оптимизации

### Размеры Docker образов
| Компонент | ДО | ПОСЛЕ | Улучшение |
|-----------|-----|-------|-----------|
| Frontend образ | ~150MB | ~15MB | **90%** ↓ |
| Время сборки frontend | 3-5 мин | 30-60 сек | **75%** ↓ |
| Первая загрузка страницы | ~5MB bundle | ~500KB bundle | **90%** ↓ |

### Производительность загрузки

**ДО (все в bundle):**
- Первая загрузка: ~5MB (медиа в JS)
- Time to Interactive: ~8s

**ПОСЛЕ (MinIO + lazy loading):**
- Первая загрузка: ~500KB (только JS/CSS)
- Time to Interactive: ~2s
- Медиа грузятся параллельно по требованию

## 🎯 Стратегия миграции

### Подход 1: Постепенная миграция (рекомендуется)
1. **Фаза 1** - Только видео (42MB)
   - animatedBackground.mp4
   - pvpArena2.mp4
   - mainCityBackground.mp4
   - Остальные видео

2. **Фаза 2** - Большие PNG (15MB)
   - Фоны dungeons
   - Большие фоны

3. **Фаза 3** - Музыка и остальное

### Подход 2: Полная миграция сразу
- Перенести все assets за один раз
- Более быстрый результат, но больше риска

## 🔧 Troubleshooting

### Проблема: CORS ошибки
**Решение:**
```bash
mc cors set cors.json myminio/elbrus-arena-assets
# Или добавить CORS headers в nginx
```

### Проблема: 404 при загрузке медиа
**Решение:**
- Проверить, что файлы загружены: `mc ls myminio/elbrus-arena-assets/`
- Проверить публичный доступ: `mc anonymous list myminio/elbrus-arena-assets`
- Проверить nginx проксирование

### Проблема: Медленная загрузка из MinIO
**Решение:**
- Настроить nginx кеширование
- Использовать CDN перед MinIO (Cloudflare)
- Оптимизировать размеры файлов (compression)

## 📝 Checklist миграции

- [ ] MinIO установлен и запущен
- [ ] Bucket создан
- [ ] CORS настроен
- [ ] Медиа загружены в MinIO
- [ ] Проверен доступ к файлам через браузер
- [ ] Переменные окружения обновлены
- [ ] Утилита assetUrl создана
- [ ] Импорты обновлены (начать с одной страницы)
- [ ] Протестировано локально
- [ ] .dockerignore обновлен
- [ ] Nginx конфиг обновлен
- [ ] Образ пересобран
- [ ] Задеплоено на production
- [ ] Проверена загрузка всех медиа
- [ ] Мониторинг производительности

## 🎨 Дополнительные оптимизации

### 1. Responsive images
```typescript
// Генерировать разные размеры для разных экранов
getAssetUrl('image.png', { size: 'thumbnail' }) // 200x200
getAssetUrl('image.png', { size: 'medium' })    // 800x800
getAssetUrl('image.png', { size: 'full' })      // original
```

### 2. WebP конверсия
MinIO может хранить как PNG, так и WebP версии
```typescript
getAssetUrl('image.png', { format: 'webp' })
```

### 3. Lazy loading компонентов
```typescript
const BackgroundVideo = lazy(() => import('./BackgroundVideo'))
```

### 4. Предзагрузка критичных ресурсов
```typescript
// В index.html или компоненте
<link rel="preload" href={getAssetUrl('hero-bg.mp4')} as="video">
```

## 💰 Экономия ресурсов

### Хранение
- Docker Registry: **-63MB на каждую версию**
- Локальный диск: **-63MB на каждую сборку**

### Сеть
- Уменьшение первой загрузки с 5MB до 500KB
- Параллельная загрузка медиа (до 6 потоков)
- Кеширование на 30 дней

### CI/CD
- Время сборки: **-75%** (с 5 минут до 1 минуты)
- Размер артефактов: **-90%**

---

**Следующий шаг:** Запустите скрипт `upload-assets-to-minio.js` для автоматической миграции!
